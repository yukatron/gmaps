<h1>Maps#index</h1>

<%= form_for(@map) do |f| %>

<%= f.label :address %>
<%= f.text_area :address, autofocus: true %>

<%= f.label :name %>
<%= f.text_area :name %>

<%= f.submit 'save' %>
<% end %>
